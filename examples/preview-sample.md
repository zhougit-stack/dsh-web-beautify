# 预览示例

这是右侧预览面板的 **Markdown 渲染示例**，用于展示 README 截图效果。

## 功能要点

- 点击消息里的文件路径、图片、网页链接、代码块即可在右侧预览
- 点击同一预览对象再次点击 = 关闭面板（开关语义）
- 每个预览对象一个标签，切换会话自动记住面板状态

## 代码示例

```js
function greet(name) {
  return `Hello, ${name}!`
}

console.log(greet('dsh'))
```

> 打开 `.md` 文件时会按上面的形式渲染，代码块保留原始文本与语法高亮。
